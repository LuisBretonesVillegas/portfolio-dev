---
interface Props {
  title?: string;
  description?: string;
}
const {
  title = 'Luis Bretones | Computer Engineering Student',
  description = 'Computer Engineering student at UAL, focused on networking and cloud infrastructure.',
} = Astro.props;
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&family=Playfair+Display:wght@700;800&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />
  <style is:global>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #f5f0e8;
      --bg-card: #ede8df;
      --text: #111111;
      --text-muted: #6b7280;
      --accent: #1a5c00;
      --border: #e5e7eb;
    }

    body {
      font-family: 'DM Sans', sans-serif;
      background-color: var(--bg);
      color: var(--text);
      min-height: 100vh;
    }

    /* ── NAV ── */
    nav {
      position: fixed;
      inset: 0 0 auto 0;
      z-index: 100;
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      background: rgba(245, 240, 232, 0.92);
      border-bottom: 1px solid var(--border);
    }
    .nav-inner {
      max-width: 860px;
      margin: 0 auto;
      padding: 1rem 2rem;
      display: grid;
      grid-template-columns: 1fr auto 1fr;
      align-items: center;
    }
    .nav-left { display: flex; flex-direction: column; gap: 0.1rem; }
    .nav-name {
      font-size: 0.95rem;
      font-weight: 600;
      color: var(--text);
      text-decoration: none;
    }
    .nav-name:hover { color: var(--accent); }
    .nav-subtitle {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.62rem;
      font-weight: 500;
      letter-spacing: 0.08em;
      color: var(--accent);
      text-transform: uppercase;
    }
    .nav-center { display: flex; align-items: center; gap: 2rem; }
    .nav-link {
      font-size: 0.85rem;
      font-weight: 500;
      color: var(--text-muted);
      text-decoration: none;
      transition: color 0.2s;
    }
    .nav-link:hover, .nav-link.active { color: var(--accent); }
    .nav-right { display: flex; align-items: center; gap: 1.25rem; justify-content: flex-end; }
    .nav-icon-link { color: var(--text-muted); display: flex; align-items: center; transition: color 0.2s; }
    .nav-icon-link:hover { color: var(--accent); }

    /* ── LAYOUT ── */
    main {
      padding-top: 65px;
      max-width: 860px;
      margin: 0 auto;
      padding-left: 2rem;
      padding-right: 2rem;
    }

    /* ── SHARED SECTION ── */
    .section { padding: 3rem 0; border-bottom: 1px solid var(--border); }
    .section:last-child { border-bottom: none; }
    .section-title {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.65rem;
      font-weight: 600;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--text-muted);
      margin-bottom: 1.75rem;
    }

    /* ── REPO FEED ── */
    .repo-feed { display: flex; flex-direction: column; gap: 0.75rem; }
    .repo-item {
      display: block;
      padding: 1rem 1.1rem;
      border: 1px solid var(--border);
      border-radius: 4px;
      background: var(--bg-card);
      text-decoration: none;
      color: var(--text);
      transition: border-color 0.15s;
    }
    .repo-item:hover { border-color: var(--accent); }
    .repo-item-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 0.5rem;
      gap: 1rem;
    }
    .repo-item-name {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--text);
      transition: color 0.2s;
    }
    .repo-item-name::before { content: '~/'; color: var(--text-muted); font-weight: 400; }
    .repo-item:hover .repo-item-name { color: var(--accent); }
    .repo-item-src {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.72rem;
      color: var(--accent);
      white-space: nowrap;
    }
    .repo-item-tags { display: flex; gap: 0.35rem; flex-wrap: wrap; margin-bottom: 0.5rem; }
    .repo-item-tag {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.65rem;
      font-weight: 500;
      padding: 0.15rem 0.5rem;
      border-radius: 3px;
      border: 1px solid color-mix(in srgb, var(--accent) 40%, transparent);
      color: var(--accent);
    }
    .repo-item-desc { font-size: 0.875rem; color: var(--text-muted); line-height: 1.6; }
    .view-all {
      display: block;
      margin-top: 0.75rem;
      padding: 0.7rem;
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.75rem;
      color: var(--accent);
      text-decoration: none;
      text-align: center;
      border: 1px dashed var(--border);
      border-radius: 4px;
      transition: border-color 0.15s;
    }
    .view-all:hover { border-color: var(--accent); }

    /* ── FOOTER ── */
    footer {
      max-width: 860px;
      margin: 0 auto;
      padding: 2rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      font-size: 0.8rem;
      color: var(--text-muted);
      flex-wrap: wrap;
      gap: 0.5rem;
      border-top: 1px solid var(--border);
    }
    .footer-links { display: flex; gap: 1.25rem; }
    .footer-links a { color: var(--text-muted); text-decoration: none; transition: color 0.2s; }
    .footer-links a:hover { color: var(--accent); }

    /* ── RESPONSIVE ── */
    @media (max-width: 768px) {
      .nav-inner {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0.875rem 1.25rem;
      }
      .nav-center { display: flex; gap: 1rem; }
      .nav-link { font-size: 0.78rem; }
      .nav-subtitle { display: none; }
      .nav-right { gap: 0.85rem; }
    }
  </style>
</head>
<body>

  <nav>
    <div class="nav-inner">
      <div class="nav-left">
        <a href="/" class="nav-name">Luis Bretones</a>
        <span class="nav-subtitle">Almería</span>
      </div>
      <div class="nav-center">
        <a href="/projects/" class="nav-link">Projects</a>
        <a href="/blog/" class="nav-link">Blog</a>
        <a href="/contact/" class="nav-link">Contact</a>
      </div>
      <div class="nav-right">
        <a href="https://github.com/LuisBretonesVillegas" target="_blank" rel="noopener" class="nav-icon-link" aria-label="GitHub">
          <svg width="18" height="18" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"/></svg>
        </a>
        <a href="https://www.linkedin.com/in/luis-bretones-villegas-02b103315/" target="_blank" rel="noopener" class="nav-icon-link" aria-label="LinkedIn">
          <svg width="18" height="18" fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
        </a>
      </div>
    </div>
  </nav>

  <main>
    <slot />
  </main>

  <footer>
    <span>© {new Date().getFullYear()} Luis Bretones</span>
    <div class="footer-links">
      <a href="https://github.com/LuisBretonesVillegas" target="_blank" rel="noopener">GitHub</a>
      <a href="https://www.linkedin.com/in/luis-bretones-villegas-02b103315/" target="_blank" rel="noopener">LinkedIn</a>
    </div>
  </footer>

</body>
</html>
